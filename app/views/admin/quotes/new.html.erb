<div class="bg-body-light">
    <div class="content content-full">
        <div class="d-flex flex-column flex-sm-row justify-content-sm-between align-items-sm-center">
            <h3 class="flex-sm-fill font-size-h2 font-w400 mt-2 mb-0 mb-sm-2">Crear Cotización</h3>
            <nav class="flex-sm-00-auto ml-sm-3" aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <%= link_to('Cotizaciones', admin_quotes_path, class: 'breadcrumb-item')%>
                    <li class="breadcrumb-item active" aria-current="page">Procedimiento</li>
                </ol>
            </nav>
        </div>
    </div>
</div>
<div class="d-flex justify-content-center">
  <div class="col-sm-12">
    <%= form_for(@quote, url: admin_quotes_path(@quote), html: {multipart: true, onsubmit:"return validateQuoteStep1()"}) do |form| %>
      <div class="card">
        <div class="card-body">
          <%= render 'admin/quotes/partials/form', form: form %>
          <%= link_to('Cancelar', admin_quotes_path, class: 'btn btn-light mt-4')%>
          <%= form.submit "Siguiente", id: 'btn_submit', class: "btn btn-success ml-sm-3 mt-4" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
